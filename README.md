# 🔒 PASSWORD MANAGER -- SECUREVAULT
SECUREVAULT is a simple cryptography fernet based program which helps in storing the password for user in ecrypted format 

# Features
SECUREVAULT provides a feature to create a new key or load a key to decrypt the hashed text file which is the password file 
it takes the key file as the neccesary component to decrypt the hashed password text if the key file is wrong it will not decrypt the password.txt

# Technologies/tools used
cryptography module 
os module 

# Steps to install & run the project
steps to install the program 

```git clone ()```

To run the project:
first you need to modules installed 
```pip install cryptography```



```os module (comes pre installed)```



```python3 password_manager.py```



        OR

        
```python password_manager.py```

# Instruction for testing 

run the file in the same directory or give the directory file path to run the code correctly 

select create a new file and choose to name your key file in your respective path (just write the name to save the file in the same path where your code is present) with .key extension

select create a new password file and give it the name of your choice (this will be the password file where all your password will be stored in encrypted format )

select load your existing password file 

select add a new password 
and fill the service name and password 

password will be saved 

select get password to get your password 

enter the service name you want password for 

select q to quit 

and then from there ownward you can load your pre exisiting key file and password file to get your password for the respective services back 





